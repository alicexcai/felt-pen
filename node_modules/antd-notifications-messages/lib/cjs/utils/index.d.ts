/// <reference types="react" />
interface AddType {
    content: JSX.Element;
    duration: number;
}
export declare const store: {
    add: ({ content, duration }: AddType) => void;
    remove: (key?: string | undefined) => void;
};
export {};
